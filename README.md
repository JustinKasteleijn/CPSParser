# CPSParser

A continuation-passing style (CPS) parser combinator library written in Haskell with built-in position tracking.

---

## Testing with Sydtest

This project uses **sydtest** for its unit testing suite. 

### Running the Tests
To execute the test suite with the multithreaded runtime system enabled, run the following command in your terminal:

```bash
cabal test
```
